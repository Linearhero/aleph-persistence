# Instance Journal

Reserved for structured per-instance journals tracking what each session instance found.
Currently the developmental instance (vessel) uses its JSON file.
Future: per-session logs that link battery reports to the session context that produced them.
